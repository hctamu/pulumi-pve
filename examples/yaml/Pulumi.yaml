name: provider-pve
runtime: yaml
plugins:
  providers:
    - name: pve
      path: ../../bin

variables:
  zone:
    name: test
    vxlanPort: 4784
    ipam: pve
    fabric: zerotest
    nodes:
      - stage-devops-proxmox02
      - stage-devops-proxmox01
    

resources:
  myVnet:
    type: pve:sdn:Vnet
    properties:
      vnet: test     # max 8 alphanumeric chars, must start with a letter
      zone: ztonvl        # must reference an existing SDN zone
      tag: 10099          # VNI/VLAN tag (1-16777215)
      alias: "my vnet"    # optional
      vlanaware: false    # optional; allow guest VLANs to trunk through
      isolatePorts: false # optional; isolate all bridge interfaces
  myZone:
    type: pve:sdn/zone:VxlanZone
    properties: ${zone}
  
  mySDNApply:
    type: pve:sdn:Apply
    properties:
      triggers:
        zone: ${zone}
        vnetTag: ${myVnet.tag}
    options:
      dependsOn:
        - ${myVnet}
        - ${myZone}

  # myFile:
  #   type: pve:file:File
  #   properties:
  #     datastoreId: cephfs
  #     contentType: snippets
  #     sourceRaw:
  #       fileData: |
  #         hello world
  #       fileName: testfile011.yaml

  # myPool:
  #   type: pve:pool:Pool
  #   properties:
  #     name: testpool
  #     comment: "a test pool"
  #     storage:
  #       - cephfs
  #     vms:
  #       - 100

  # myVM:
  #   type: pve:vm:VM
  #   properties:
  #     name: "aTestVM"
  #     tags:
  #       - "tag2"
  #       - "tag1"
  #     description: "test VM"
  #     cpu:
  #       type: EPYC-v3
  #       flagsEnabled:
  #         - virt-ssbd
  #       flagsDisabled:
  #         - spec-ctrl
  #       hidden: true
  #       hvVendorId: AuthenticAMD
  #       physBits: "48"
  #       cores: 4
  #       sockets: 2
  #       limit: 3.0
  #       units: 2048
  #       vcpus: 3
  #       numa: true
  #       numaNodes:
  #         - cpus: "0-1"
  #           hostNodes: "0"
  #           memory: 2048
  #           policy: "preferred"
  #         - cpus: "2-3"
  #           hostNodes: "1"
  #           policy: "interleave"
  #     memory: 27875
  #     efidisk:
  #       storage: "ceph-ha"
  #       efitype: "4m"
  #       preEnrolledKeys: false
  #     disks:
  #       - storage: "ceph-ha"
  #         size: 20
  #         interface: "scsi0"
  #       - storage: "ceph-ha"
  #         size: 17
  #         interface: "scsi1"
  #       - storage: "ceph-ha"
  #         size: 21
  #         interface: "sata0"
  #     clone:
  #       vmId: 102
  #       timeout: 360
  #       fullClone: true
