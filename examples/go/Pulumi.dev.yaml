encryptionsalt: encryptionsalt
config:
  pve:pveUrl: pve_api_endpoint
  pve:pveUser: pve_user
  pve:pveToken:
    secure: encrypted_pve_token
  # pve:insecureSkipVerify: false # Set to true to disable TLS certificate verification
  provider-pve:name: dev
